import LoginPage from "@/components/Login/LoginPage";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <LoginPage />
    </div>
  );
};

export default page;
